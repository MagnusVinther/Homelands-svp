import React from 'react'
import { AppRouter } from '../App/Router/Router'

export const Main = () => {
  return (
    <main className='mainContainer'>
        <AppRouter>
            
        </AppRouter>
    </main>
  )
}
